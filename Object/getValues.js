const students = {
    id:"govinda",
    age:28,
    marks:94.4,
    city:"Banglore"
};
console.log(students["id"]);    
console.log(students.age);

// we can use both above to get values
// any value change into string in javascript.
const obj={
    1:"a",
    2:"b",
    true:"c",
    null:"d",
    undefined:"e"
}